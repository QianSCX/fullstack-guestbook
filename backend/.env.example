# 端口（默认 3001）
PORT=3001

# 监听地址（0.0.0.0 表示允许局域网/公网访问；仅本机调试用 127.0.0.1）
HOST=0.0.0.0

# 不设置 DATABASE_URL 时，使用 SQLite（默认，零依赖，自动建表）
# 设置后使用 PostgreSQL，例如：
# DATABASE_URL=postgres://guestbook:guestbook@localhost:5432/guestbook
